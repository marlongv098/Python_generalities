

a_list=['spam!', [1], 'one', ['Brie', 'Roquefort', 'Pol le Veq'], [1, 2, 3]]

def count_list(a_list):		# THIS FUNCTION TAKES A LIST AND COUNT THE AMOUNT OF ELEMENTS IN EVERY INDEX. THE PROBLEM HERE BECOMES IF ONE THE INDEX IS A INTEGER NUMBER THE "len" FUNCTION DOES NOT WORKS WELL.I AVOIDED THE ERROR USING A NUMBER LIKE A LIST.BUT IS NOT FIXED.
	for index, value in enumerate(a_list):
	  a_list[index]=len(value)
	print a_list

count_list(a_list)
